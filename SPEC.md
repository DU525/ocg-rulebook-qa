# 游戏王OCG规则问答系统技术规格说明书

## 一、项目概述与愿景

本项目旨在构建一个基于检索增强生成（RAG）技术的游戏王OCG规则智能问答系统。系统以游戏王OCG官方规则书为核心知识库，结合用户自定义规则扩展，实现精准、可溯源的规则问答服务。

**核心价值主张**：
- **准确性**：所有回答均基于权威规则文档，并精确引用原文段落
- **可扩展性**：支持用户上传自定义规则文档，灵活扩展知识库
- **透明性**：回答附带来源标注，用户可追溯信息出处
- **实用性**：提供对话式交互，支持多轮上下文问答

## 二、技术架构设计

### 2.1 系统架构概览

```
┌─────────────────────────────────────────────────────────────┐
│                      前端展示层                             │
│  (React + TypeScript + TailwindCSS)                        │
├─────────────────────────────────────────────────────────────┤
│                      API网关层                              │
│  (Flask REST API / FastAPI)                                │
├─────────────────────────────────────────────────────────────┤
│                      业务逻辑层                             │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐             │
│  │  RAG引擎   │ │  对话管理   │ │  文档处理   │             │
│  │ (LangChain)│ │ (ChatMgr)  │ │ (Processor)│             │
│  └─────────────┘ └─────────────┘ └─────────────┘             │
├─────────────────────────────────────────────────────────────┤
│                      数据存储层                             │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐             │
│  │  ChromaDB   │ │  SQLite     │ │  文件系统   │             │
│  │ (向量数据库)│ │ (结构化数据)│ │ (文档存储) │             │
│  └─────────────┘ └─────────────┘ └─────────────┘             │
├─────────────────────────────────────────────────────────────┤
│                      外部服务层                             │
│  (OpenAI API / Claude API / 本地LLM)                       │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 技术栈选择

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 前端框架 | React 18 + TypeScript | 类型安全，组件化开发 |
| UI库 | TailwindCSS + HeadlessUI | 轻量级样式，可访问组件 |
| 状态管理 | Zustand | 轻量级状态管理 |
| 后端框架 | Flask | 轻量级Python Web框架 |
| RAG框架 | LangChain | 模块化RAG组件 |
| 向量数据库 | ChromaDB | 轻量级嵌入式向量数据库 |
| 嵌入模型 | sentence-transformers | 多语言嵌入模型 |
| LLM | OpenAI GPT-4 / Claude | 大语言模型API |
| 文档解析 | PyMuPDF + python-docx | PDF/DOCX解析 |
| 数据库 | SQLite | 轻量级结构化存储 |

### 2.3 数据流程设计

```
用户问题 → 向量化 → 向量检索 → 上下文组装 → LLM生成 → 回答输出
    ↑                            ↓
    └─── 引用文档片段 ← 源码追溯 ←┘
```

## 三、功能模块详细设计

### 3.1 文档管理模块

**功能需求**：

| 功能 | 描述 | 优先级 |
|------|------|--------|
| 规则书导入 | 自动下载并解析OCG规则书文档 | P0 |
| 文档上传 | 支持用户上传PDF/DOCX/TXT格式规则文档 | P0 |
| 文档解析 | 提取文本内容，保持原始结构 | P0 |
| 分块处理 | 智能文档分块，保留语义完整性 | P0 |
| 向量化 | 生成文档块向量，存储至ChromaDB | P0 |
| 文档管理 | 查看、新增、删除自定义文档 | P1 |

**文档分块策略**：

```
┌────────────────────────────────────────────┐
│ 原始文档                                    │
├────────────────────────────────────────────┤
│ 章节标题                                    │
│   小节标题                                  │
│     段落1 (规则内容)                        │
│     段落2 (解释说明)                        │
│     表格1 (规则对照)                        │
│     列表项                                  │
└────────────────────────────────────────────┘
           ↓
┌────────────────────────────────────────────┐
│ 分块结果                                    │
├────────────────────────────────────────────┤
│ Chunk 1: 章节标题 + 段落1 [重叠32字]         │
│ Chunk 2: 段落2 + 表格1 [重叠32字]            │
│ Chunk 3: 表格2 + 列表项 [重叠32字]           │
└────────────────────────────────────────────┘
```

**配置参数**：

```yaml
chunking:
  chunk_size: 512          # 每块字符数
  chunk_overlap: 64        # 重叠字符数
  min_chunk_length: 100    # 最小块长度

embedding:
  model: "paraphrase-multilingual-MiniLM-L12-v2"
  dimension: 384
```

### 3.2 RAG检索模块

**检索流程**：

1. **查询向量化**：将用户问题转换为向量表示
2. **相似度检索**：在ChromaDB中检索最相关的文档块
3. **重排序**：使用交叉编码器对候选文档块重排序
4. **上下文组装**：组装检索结果和原始问题

**检索参数配置**：

```yaml
retrieval:
  top_k: 5                 # 检索返回条数
  similarity_threshold: 0.7 # 相似度阈值
  rerank: true             # 是否启用重排序

rag:
  max_context_length: 4096 # 最大上下文长度
  include_citations: true   # 包含引用标注
```

### 3.3 回答生成模块

**Prompt模板设计**：

```
【系统提示】
你是一个专业的游戏王OCG规则问答助手。你的任务是根据提供的上下文信息，准确回答用户关于游戏王OCG规则的问题。

【回答要求】
1. 基于提供的上下文信息进行回答，不要编造信息
2. 回答必须引用相关的规则条款，格式：[来源：章节标题，第X段]
3. 对于不确定的问题，明确告知用户
4. 回答应该清晰、准确、专业

【上下文信息】
{context}

【用户问题】
{question}

【回答】
```

**回答格式**：

```json
{
  "answer": "根据OCG规则，通常召唤1回合只能进行1次。等级5·6的怪兽需要解放1只怪兽，等级7以上的怪兽需要解放2只怪兽。[来源：大师规则·卡片·怪兽的召唤·通常召唤]",
  "citations": [
    {
      "source": "c02/卡片.rst",
      "chunk_id": "chunk_xxx",
      "text": "通常召唤...1回合只能进行1次...",
      "position": {"start": 120, "end": 200}
    }
  ],
  "confidence": 0.95
}
```

### 3.4 对话管理模块

**对话状态**：

```python
class Conversation:
    id: str                          # 对话唯一标识
    messages: List[Message]          # 消息列表
    created_at: datetime             # 创建时间
    updated_at: datetime             # 更新时间
    metadata: Dict                   # 元数据

class Message:
    id: str                          # 消息唯一标识
    role: Literal["user", "assistant"] # 角色
    content: str                     # 内容
    citations: List[Citation]        # 引用列表
    timestamp: datetime              # 时间戳
```

**对话历史策略**：

- 保留最近10轮对话作为上下文
- 支持多会话管理
- 支持对话搜索和导出

### 3.5 日志记录模块

**日志类型**：

| 日志类型 | 记录内容 | 保留时间 |
|----------|----------|----------|
| 请求日志 | 用户问题、响应时间、状态码 | 30天 |
| 对话日志 | 对话内容、引用信息 | 永久 |
| 错误日志 | 异常信息、堆栈跟踪 | 90天 |
| 性能日志 | API响应时间、资源使用 | 7天 |

**日志格式**：

```json
{
  "timestamp": "2024-01-15T10:30:00Z",
  "level": "INFO",
  "type": "request",
  "data": {
    "conversation_id": "conv_xxx",
    "question": "什么是通常召唤？",
    "response_time_ms": 1234,
    "status": "success",
    "tokens_used": 1500
  }
}
```

### 3.6 性能监控模块

**监控指标**：

```
┌─────────────────────────────────────────────┐
│              系统性能仪表盘                  │
├─────────────────────────────────────────────┤
│ 响应时间指标                                  │
│  ├─ 平均响应时间: 1.2s                       │
│  ├─ P95响应时间: 2.5s                        │
│  └─ P99响应时间: 4.8s                        │
├─────────────────────────────────────────────┤
│ 请求统计                                      │
│  ├─ 总请求数: 10,234                         │
│  ├─ 今日请求: 523                            │
│  └─ 错误率: 0.5%                             │
├─────────────────────────────────────────────┤
│ 资源使用                                      │
│  ├─ CPU使用率: 45%                           │
│  ├─ 内存使用率: 62%                          │
│  └─ 向量数据库大小: 128MB                     │
├─────────────────────────────────────────────┤
│ RAG性能                                       │
│  ├─ 检索平均耗时: 120ms                      │
│  └─ 生成平均耗时: 1.1s                       │
└─────────────────────────────────────────────┘
```

## 四、数据库设计

### 4.1 向量数据库（ChromaDB）

**Collection: ocg_rules**

```python
{
    "ids": ["chunk_001", "chunk_002", ...],
    "embeddings": [[0.123, -0.456, ...], ...],  # 384维向量
    "documents": ["规则内容文本...", "..."],
    "metadatas": [
        {
            "source": "c02/卡片.rst",
            "chapter": "卡片",
            "section": "怪兽的召唤",
            "chunk_index": 0,
            "char_count": 512
        },
        ...
    ]
}
```

### 4.2 关系数据库（SQLite）

**表结构设计**：

```sql
-- 对话表
CREATE TABLE conversations (
    id TEXT PRIMARY KEY,
    title TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    metadata JSON
);

-- 消息表
CREATE TABLE messages (
    id TEXT PRIMARY KEY,
    conversation_id TEXT REFERENCES conversations(id),
    role TEXT CHECK(role IN ('user', 'assistant')),
    content TEXT,
    citations JSON,
    tokens_used INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 文档表
CREATE TABLE documents (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    source TEXT,                      -- 'builtin' | 'uploaded'
    file_path TEXT,
    status TEXT CHECK(status IN ('pending', 'processing', 'completed', 'failed')),
    chunk_count INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 性能日志表
CREATE TABLE performance_logs (
    id TEXT PRIMARY KEY,
    endpoint TEXT,
    method TEXT,
    response_time_ms REAL,
    status_code INTEGER,
    tokens_used INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 系统配置表
CREATE TABLE system_config (
    key TEXT PRIMARY KEY,
    value TEXT,
    description TEXT,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

## 五、前端界面设计

### 5.1 页面结构

```
┌─────────────────────────────────────────────────────────────┐
│  Logo  游戏王OCG规则问答    [文档管理] [历史记录] [监控]   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │                   欢迎使用OCG规则问答系统             │    │
│  │              基于官方规则书的智能问答助手              │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  [User] 如何进行通常召唤？                           │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  [Assistant] 通常召唤是怪兽卡的基本召唤方式...        │    │
│  │  [来源：c02/卡片.rst - 怪兽的召唤·特殊召唤]          │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ [输入您的问题...]                        [发送]     │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 交互设计规范

**组件规范**：

| 组件 | 状态 | 样式描述 |
|------|------|----------|
| 消息气泡 | 用户消息 | 右对齐，蓝色背景（#3B82F6） |
| 消息气泡 | 助手消息 | 左对齐，白色背景，阴影效果 |
| 引用高亮 | 悬停态 | 背景高亮（#FEF3C7），显示完整引用 |
| 输入框 | 默认态 | 白色背景，灰色边框（#E5E7EB） |
| 输入框 | 聚焦态 | 蓝色边框（#3B82F6），阴影效果 |
| 发送按钮 | 默认态 | 蓝色背景（#3B82F6），白色文字 |
| 发送按钮 | 悬停态 | 深蓝色背景（#2563EB） |
| 发送按钮 | 加载态 | 显示旋转加载图标 |

**动画效果**：

- 消息出现：translateY(20px) → translateY(0)，opacity 0 → 1，duration 300ms
- 加载状态：脉冲动画，scale 1 → 1.05 → 1，duration 600ms
- 引用展开：maxHeight 0 → auto，duration 200ms

### 5.3 响应式设计

**断点设计**：

| 断点 | 宽度范围 | 布局调整 |
|------|----------|----------|
| 移动端 | < 640px | 单列布局，简化导航 |
| 平板端 | 640px - 1024px | 双列可选，侧边栏收起 |
| 桌面端 | > 1024px | 双列布局，完整功能 |

## 六、API接口设计

### 6.1 REST API端点

```
基础路径: /api/v1

┌─────────────────────────────────────────────────────────────┐
│ 问答接口                                                     │
├─────────────────────────────────────────────────────────────┤
│ POST /chat/question                                         │
│   Request: { question: string, conversation_id?: string } │
│   Response: { answer, citations, conversation_id }         │
├─────────────────────────────────────────────────────────────┤
│ 文档管理接口                                                 │
├─────────────────────────────────────────────────────────────┤
│ GET  /documents                     # 获取文档列表            │
│ POST /documents/upload              # 上传文档              │
│ DELETE /documents/{id}              # 删除文档              │
│ POST /documents/{id}/reindex        # 重新索引文档          │
├─────────────────────────────────────────────────────────────┤
│ 对话管理接口                                                 │
├─────────────────────────────────────────────────────────────┤
│ GET  /conversations                 # 获取对话列表           │
│ GET  /conversations/{id}            # 获取对话详情           │
│ DELETE /conversations/{id}           # 删除对话              │
├─────────────────────────────────────────────────────────────┤
│ 监控接口                                                     │
├─────────────────────────────────────────────────────────────┤
│ GET  /metrics                       # 获取系统指标           │
│ GET  /metrics/performance          # 获取性能统计           │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 API响应格式

**成功响应**：

```json
{
  "success": true,
  "data": { ... },
  "timestamp": "2024-01-15T10:30:00Z"
}
```

**错误响应**：

```json
{
  "success": false,
  "error": {
    "code": "DOCUMENT_NOT_FOUND",
    "message": "指定的文档不存在",
    "details": { "document_id": "xxx" }
  },
  "timestamp": "2024-01-15T10:30:00Z"
}
```

## 七、安全性设计

### 7.1 输入安全

- **XSS防护**：对所有用户输入进行HTML转义
- **SQL注入防护**：使用参数化查询
- **文件上传安全**：
  - 限制文件类型（PDF/DOCX/TXT）
  - 限制文件大小（最大50MB）
  - 文件名净化处理

### 7.2 API安全

- **请求限流**：每分钟100次请求限制
- **输入验证**：Pydantic模型验证
- **敏感信息过滤**：日志中过滤敏感信息

## 八、部署架构

### 8.1 部署方案

```
┌─────────────────────────────────────────────────────────────┐
│                    生产环境部署架构                          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐               │
│  │ Nginx   │───▶│ Gunicorn │───▶│ Flask    │               │
│  │ 反向代理 │    │ 应用服务器│    │ App      │               │
│  └─────────┘    └─────────┘    └─────────┘               │
│       │                             │                       │
│       │                             ▼                       │
│       │              ┌──────────────────────────┐          │
│       │              │  Python进程              │          │
│       │              │  ├─ Flask App            │          │
│       │              │  ├─ LangChain RAG        │          │
│       │              │  └─ Background Tasks    │          │
│       │              └──────────────────────────┘          │
│       │                       │                             │
│       ▼                       ▼                             │
│  ┌─────────┐          ┌─────────────┐                    │
│  │ 静态资源 │          │  ChromaDB   │                    │
│  │ (前端)   │          │  SQLite     │                    │
│  └─────────┘          └─────────────┘                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 8.2 环境变量配置

```bash
# API配置
OPENAI_API_KEY=sk-xxx                    # OpenAI API密钥
API_BASE_URL=https://api.openai.com     # API基础URL
MODEL_NAME=gpt-4                        # 模型名称

# 数据库配置
CHROMA_DB_PATH=./data/chroma_db         # ChromaDB路径
SQLITE_DB_PATH=./data/ocg_qa.db         # SQLite路径

# 应用配置
SECRET_KEY=your-secret-key              # 应用密钥
DEBUG=false                             # 调试模式
MAX_UPLOAD_SIZE=52428800               # 最大上传文件大小

# 向量配置
EMBEDDING_MODEL=sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2
EMBEDDING_DEVICE=cpu                    # 或 cuda
```

## 九、开发路线图

### 阶段一：基础架构（第1-2周）

- [ ] 项目初始化，目录结构创建
- [ ] 后端基础框架搭建（Flask）
- [ ] OCG规则书数据下载与解析
- [ ] 文档分块与向量化流程实现
- [ ] ChromaDB集成与基础检索

### 阶段二：RAG核心（第3-4周）

- [ ] LangChain RAG链实现
- [ ] Prompt模板设计与优化
- [ ] 引用追踪与溯源功能
- [ ] LLM API集成与调用

### 阶段三：前端开发（第5-6周）

- [ ] React项目初始化
- [ ] 问答界面开发
- [ ] 对话历史管理
- [ ] 文档管理界面上传/删除

### 阶段四：企业级功能（第7-8周）

- [ ] 日志系统完善
- [ ] 性能监控仪表盘
- [ ] 系统配置管理
- [ ] 部署脚本编写

### 阶段五：测试与优化（第9-10周）

- [ ] 单元测试编写
- [ ] 集成测试
- [ ] 性能优化
- [ ] 文档完善

## 十、质量保证

### 10.1 测试策略

| 测试类型 | 覆盖率目标 | 工具 |
|----------|------------|------|
| 单元测试 | 80%+ | pytest |
| 集成测试 | 70%+ | pytest + requests |
| E2E测试 | 核心流程 | Playwright |

### 10.2 性能基准

| 指标 | 目标值 |
|------|--------|
| API响应时间（P95） | < 3秒 |
| 检索召回率 | > 85% |
| 并发用户支持 | 50+ |

### 10.3 代码规范

- **Python**：遵循PEP 8，使用Black格式化
- **TypeScript**：遵循Airbnb ESLint规则
- **Git提交**：使用Conventional Commits规范

## 十一、附录

### 11.1 OCG规则书目录结构

```
docs/
├── c01/                    # 变更的规则&决斗的基本概念
│   ├── 规则变动.rst
│   ├── 特选规则.rst
│   └── 决斗的基本概念.rst
├── c02/                    # 大师规则
│   ├── OCG的概要.rst
│   ├── 决斗进行的场所.rst
│   ├── 卡片.rst
│   ├── 卡片的效果.rst
│   ├── 效果以外的文本.rst
│   ├── 游戏的进行.rst
│   └── 大会规则.rst
└── c03/                    # 检定测试
    └── 游戏王OCG检定测试模拟试验.rst
```

### 11.2 术语表

| 术语 | 英文 | 说明 |
|------|------|------|
| 检索增强生成 | RAG | Retrieval-Augmented Generation |
| 向量嵌入 | Embedding | 将文本转换为向量表示 |
| 文档分块 | Chunking | 将长文档分割成小块 |
| 咒文速度 | Spell Speed | 卡片效果响应速度等级 |
| 召唤 | Summon | 将怪兽放置到场上 |

---

**文档版本**：v1.0
**创建日期**：2024年1月15日
**作者**：MiniMax Agent